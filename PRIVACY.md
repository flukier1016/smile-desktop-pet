# 隐私说明

生效日期：2026-07-28

笑笑桌宠是一款完全离线的 macOS 桌面应用。v1.2.0 起提供场景感知与可选的本地屏幕 OCR。

## 不收集的数据

App 不收集、存储或上传以下内容：

- 个人身份信息；
- 设备标识符；
- 使用统计或分析事件；
- 文件、照片、通讯录、定位或剪贴板内容；
- 崩溃日志或诊断数据。

App 不使用相机或麦克风，也不会把屏幕内容作为用户数据收集、保存或上传。

## 网络

App 不包含网络请求代码，不连接任何服务器，也不包含广告、分析 SDK 或自动更新 SDK。

## 默认场景感知

“自动感知”默认开启，只读取 macOS 提供的当前前台 App 名称与 Bundle Identifier，用于判断写代码、表格、会议、消息等场景。

- 不读取文档内容；
- 不读取聊天内容；
- 不截图；
- 不需要屏幕录制权限；
- 所有判断都在本机进程内完成。

用户可以随时从“菜单栏笑脸 → 场景感知”关闭自动感知或暂停 30 分钟。

## 可选的本地屏幕 OCR

“本地屏幕 OCR”默认关闭。只有用户主动点击开启后，App 才会请求 macOS“屏幕与系统音频录制”权限。

开启后：

- 每次只截取当前前台 App 最前方的一个窗口，不截取完整桌面；
- 通过 macOS Vision 框架在本机内存中识别中英文文字；
- 用识别结果判断报错、测试通过、论文、视频、购物等更细场景；
- 不写入截图文件；
- 不保存 OCR 文字；
- 不记录识别历史；
- 不通过网络发送任何内容；
- 当前 App 切换后会丢弃上一窗口的临时识别结果。

用户可选择 8 秒、15 秒或 30 秒识别间隔，并可随时关闭 OCR。撤销系统权限的路径为“系统设置 → 隐私与安全性 → 屏幕与系统音频录制”。

## 保存到本机的设置

App 仅通过 macOS `UserDefaults` 保存以下设置：

- 桌宠窗口位置；
- 桌宠大小；
- 是否启用自动感知；
- 是否启用本地 OCR；
- OCR 识别间隔。

App 不保存前台 App 历史、窗口标题、截图、OCR 文字或状态切换历史。安静模式、鼠标穿透和临时隐藏状态不会上传。

清除本地设置：

```bash
defaults delete com.local.smile-desktop-pet
```

## 人物图片

原始人物照片仅用于本次 Q 版角色生成，不包含在本仓库、App 或 Release 中。发行包只包含生成后的角色素材。

## 变更

如果未来版本加入联网、自动更新或遥测功能，必须先更新本文件并在 Release Notes 中明确说明。默认立场仍是关闭遥测、最小权限和本地优先。
