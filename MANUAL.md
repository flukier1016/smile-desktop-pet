# 笑笑桌宠用户使用手册

版本：1.3.0
适用系统：macOS 13 Ventura 或更高版本
适用设备：Apple Silicon 与 Intel Mac

## 1. 安装

### 推荐：DMG

1. 在 GitHub Releases 下载 `SmilePet-v1.3.0-macos-universal.dmg`。
2. 双击打开 DMG。
3. 把“笑笑桌宠”拖到“应用程序”文件夹。
4. 推出 DMG。

### 备用：ZIP

1. 下载 `SmilePet-v1.3.0-macos-universal.zip`。
2. 双击解压。
3. 把“笑笑桌宠”拖到“应用程序”文件夹。

## 2. 第一次打开

当前版本尚未进行 Apple 公证，因此直接双击时可能出现“无法验证开发者”提示。这不代表 App 需要额外权限。

正确打开方式：

1. 在 Finder 打开“应用程序”。
2. 找到“笑笑桌宠”。
3. 按住 Control 点击或直接右键。
4. 选择“打开”。
5. 在确认框中再次选择“打开”。

以后可直接双击启动。建议只从本仓库的官方 Release 下载。

## 3. 基本玩法

| 操作 | 效果 |
| --- | --- |
| 单击头部 | 摸摸头、冒爱心、随机回应 |
| 单击身体 | 弹跳并说一句 |
| 双击 | 旋转、撒彩带、播放轻提示音 |
| 按住拖拽 | 移动到桌面任意位置 |
| 右键桌宠 | 打开互动菜单 |
| 按住 Option 滚轮 | 连续放大或缩小 |

桌宠位置会自动保存。重新启动后，她会回到上次的位置。

默认大小为原版的约 75%。在右键菜单或菜单栏的“调整大小”中，可以选择：

- **迷你 60%**：适合小屏幕或长期放在角落。
- **标准 75%**：新版默认大小。
- **大只 100%**：恢复首发版本大小。

自定义尺寸会自动保存，下次启动继续沿用。

## 4. 右键互动菜单

- **喂她一口**：随机出现饼干、草莓、团子、饺子或蛋糕。
- **夸夸她**：触发开心回应、摇摆与爱心特效。
- **今日小运气**：随机抽取一句轻量运势。
- **出去散个步**：桌宠会跳到当前屏幕的另一个位置。
- **调整大小**：选择迷你、标准或大只尺寸。
- **躲 5 分钟**：桌宠暂时消失，五分钟后自动回来。
- **退出**：完全关闭桌宠。

## 5. 菜单栏笑脸

桌宠运行时，macOS 顶部菜单栏会出现笑脸图标：

- **打开控制中心**：统一管理当前状态、感知方式、大小、OCR 节奏和隐私状态。
- **叫她回来**：把桌宠召回鼠标所在屏幕的右下角。
- **让她说句话**：立即触发随机台词。
- **场景感知**：管理自动切换、本地 OCR、识别间隔、暂停与手动状态预览。
- **鼠标穿透**：鼠标操作会穿过桌宠窗口，适合看内容或点击被遮挡区域。开启后仍可从菜单栏关闭穿透。
- **安静 10 分钟**：暂停自动碎碎念和点击回应；再次点击可提前结束。
- **退出笑笑桌宠**：完全关闭 App。

### 控制中心

控制中心是 v1.3.0 的主要设置入口。顶部状态卡会显示当前场景、前台 App、
识别来源以及自动／暂停状态。下方可以直接：

- 开关自动感知；
- 开关本地屏幕 OCR，并查看权限是否生效；
- 选择迷你 60%、标准 75%、大只 100%；
- 选择 8 秒、15 秒或 30 秒 OCR 识别间隔；
- 立即识别、叫她回来、查看隐私说明。

控制中心底部的绿色提示表示识别在本机完成。关闭控制中心不会退出桌宠。

## 6. 场景感知

### 默认模式：前台 App 感知

自动感知默认开启，不需要额外权限。桌宠每四秒查看一次当前前台 App，并自动切换状态。

| 场景 | 常见 App 或条件 | 桌宠状态 |
| --- | --- | --- |
| 编程 | Codex、Claude、ChatGPT、Xcode、VS Code、终端等 | 认真写代码 |
| 报错 | 编程 App 中出现构建失败、测试失败、异常等文字 | 报错抢救中 |
| 成功 | 编程 App 中出现测试通过、构建成功等文字 | 胜利撒花 |
| 表格 | Excel、Numbers | 表格苦工 |
| 金融 | Bloomberg、Wind、TradingView、IBKR 等 | 盯盘模式 |
| 长时间工作 | 连续使用编程、表格或写作 App 约 50 至 75 分钟 | 工位服刑 |
| 写作与阅读 | Word、Pages、Notion、Preview、Books 等 | 奋笔疾书／安静读书 |
| 研究与汇报 | 论文网站、PowerPoint、Keynote 等 | 资料侦探／汇报冲刺 |
| 沟通 | Zoom、Teams、微信、Slack、Mail 等 | 开会静音／消息轰炸／邮件清零 |
| 创作 | Figma、Sketch、Photoshop 等 | 灵感施工 |
| 娱乐 | 视频、音乐、游戏、购物网站 | 快乐摸鱼／跟着摇摆／游戏助威／购物冷静期 |
| 离开 | 键盘和鼠标超过五分钟无操作 | 等你回来 |
| 深夜 | 23:00 至次日 06:00，且没有更明确的工作或娱乐场景 | 深夜劝退 |

App 切换后通常会在四秒内更新状态。状态改变时，徽章、颜色、动作、漂浮符号、道具和台词会一起变化。

### 可选模式：本地屏幕 OCR

仅靠 App 名称无法区分浏览器中的论文、视频、购物页面，也无法判断代码是否报错。因此可以主动开启本地 OCR：

1. 点击菜单栏笑脸。
2. 打开“场景感知”。
3. 点击“本地屏幕 OCR”。
4. 在 macOS 弹窗中允许“屏幕与系统音频录制”。
5. 如果授权没有立即生效，退出并重新打开笑笑桌宠。

OCR 每次只截取当前前台 App 的最前方窗口，通过 macOS Vision 在本机内存中识别文字。截图和识别文字不会保存、不会上传，也不会写入状态历史。

识别间隔可选：

- **灵敏 · 8 秒**：切换最快，CPU 使用略高。
- **均衡 · 15 秒**：默认推荐。
- **省电 · 30 秒**：适合长时间运行。

### 暂停与手动预览

- **立即识别一次**：不等待下一轮计时，马上刷新状态。
- **暂停感知 30 分钟**：保持当前状态，不再读取前台 App 或截图。
- **恢复自动切换**：结束暂停或手动预览。
- **手动预览状态**：可查看全部 23 种状态；选择后会暂时停止自动切换。
- **关闭自动感知**：桌宠回到普通陪伴模式。

### 撤销屏幕权限

进入“系统设置 → 隐私与安全性 → 屏幕与系统音频录制”，关闭“笑笑桌宠”。随后在桌宠菜单中关闭“本地屏幕 OCR”。

## 7. 开机自动启动

本版本不会自行添加开机启动项。若你希望每次登录自动运行：

1. 打开“系统设置”。
2. 进入“通用”→“登录项”。
3. 在“登录时打开”区域点击“+”。
4. 选择“应用程序”中的“笑笑桌宠”。

移除方式相同：在登录项中选中它并点击“-”。

## 8. 更新

1. 从 Releases 下载新版本。
2. 从菜单栏退出旧版本。
3. 用新 App 替换“应用程序”中的旧 App。
4. 重新打开。

桌宠位置保存在 macOS 用户偏好中，正常替换 App 不会清除。

## 9. 卸载

1. 从菜单栏退出笑笑桌宠。
2. 在“应用程序”中把 App 移到废纸篓。

如需同时清除保存的位置和设置，可在“终端”运行：

```bash
defaults delete com.local.smile-desktop-pet
```

这条命令只删除笑笑桌宠的本地偏好，不会删除其他文件。

## 10. 常见问题

### 提示无法验证开发者

按“第一次打开”章节，通过 Finder 右键选择“打开”。当前版本使用临时签名，尚未进行 Apple 公证。

### 桌宠消失了

先查看菜单栏笑脸，选择“叫她回来”。如果刚刚选择了“躲 5 分钟”，也可以立即从菜单栏召回。

### 桌宠挡住点击

在菜单栏笑脸中开启“鼠标穿透”。需要再次互动时，从同一菜单关闭穿透。

### 桌宠不说话

检查菜单栏中的“安静 10 分钟”是否开启。关闭后会恢复互动。

### 没有声音

桌宠只使用 macOS 自带的轻提示音。请检查系统音量和当前输出设备；静音不影响动画和台词。

### 打开后没有 Dock 图标

这是正常设计。桌宠不占 Dock，通过顶部菜单栏笑脸管理。

### 自动状态没有切换

确认“菜单栏笑脸 → 场景感知 → 自动感知”已经勾选。切换 App 后等待约四秒，或点击“立即识别一次”。如果正在手动预览或暂停，先点击“恢复自动切换”。

### 浏览器一直显示网上冲浪

默认模式只能知道当前使用浏览器，无法读取网页内容。开启“本地屏幕 OCR”后，桌宠才能区分论文、视频、购物、报错和测试通过等内容。

### 已经允许屏幕权限但 OCR 仍未生效

完全退出笑笑桌宠后重新打开。若仍无效，请在系统设置中关闭再重新开启该权限，然后再次启动 App。

### OCR 会保存我的聊天或文档吗

不会。OCR 画面和识别文字只在运行内存中短暂存在，不写入磁盘、不上传，也不记录历史。对于敏感窗口，可以随时关闭 OCR 或暂停感知。

### 系统版本过低

当前版本要求 macOS 13 或更高版本。旧系统无法保证运行。

## 11. 隐私与权限

App 不联网、不含遥测，不使用相机、麦克风、定位、通讯录或用户文件。默认感知只读取前台 App 名称；屏幕 OCR 默认关闭，只有用户主动开启后才请求权限。运行时只保存桌宠位置、大小和感知开关等本地设置，不保存前台 App 历史、窗口标题、截图或 OCR 文字。完整说明见 [PRIVACY.md](PRIVACY.md)。

## 12. Codex 笑笑皮肤

源码仓库附带 Codex 原生笑笑皮肤。它包含 88 帧桌宠动画和配套明暗主题，
不修改 Codex 官方 App 包。开发者或源码用户可在仓库目录运行：

```bash
./scripts/install-codex-skin.sh
```

安装程序会先备份 `~/.codex/config.toml`。恢复原外观：

```bash
./scripts/uninstall-codex-skin.sh
```

Codex 正在运行时，部分外观可能即时刷新；桌宠未刷新时，重新打开 Codex。
完整说明见 [CodexSkin/README.md](CodexSkin/README.md)。

## 13. 反馈问题

提交 Issue 时请附上：

- Mac 芯片：Apple Silicon 或 Intel；
- macOS 版本；
- 复现步骤；
- 实际结果与期望结果；
- 截图或录屏，注意先隐藏个人信息。

请勿在 Issue 中上传私人照片、账号信息、聊天记录或其他敏感内容。
